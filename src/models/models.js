const User = require('./user');
const Chat = require('./chat');
const ChatMembership = require('./chatMembership');

module.exports = {
  UserModel: User,
  ChatModel: Chat,
  ChatMembershipModel: ChatMembership,
};
